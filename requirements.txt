# Core ML
numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3

# Deep Learning
torch>=2.1

# Recommendation — implicit ALS (Fix-6: replaces custom numpy ALS)
# CPU-only:  pip install implicit
# CUDA:      pip install implicit[gpu]
implicit>=0.7.2

# Experiment tracking
mlflow>=2.10

# API
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6

# Utilities
tqdm>=4.66
